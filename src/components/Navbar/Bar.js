import styled from 'styled-components'

const Bar = styled.div`
  align-items: center;
  background-color: rgb(0, 0, 0);
  display: flex;
  height: 60px;
  padding: 0 2rem;
  position: fixed;
  width: 100%;
  z-index: 100;
`

export default Bar