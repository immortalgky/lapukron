import styled from 'styled-components'

const Bar = styled.div`
  align-items: center;
  background-color: rgba(0, 0, 0, 0.9);
  display: flex;
  height: 60px;
  padding: 0 2rem;
  position: fixed;
  width: 100%;
  z-index: 99;
`

export default Bar